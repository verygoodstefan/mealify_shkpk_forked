import 'package:flutter/material.dart';

class Ideas extends StatelessWidget {
  const Ideas({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SafeArea(child: Text('Ideas page'));
  }
}
